#!/bin/bash
# Post-create script — runs once after container is created
# Add any one-time setup here (e.g. pulling tools, configuring git hooks)

echo "✅ DevContainer ready. Repos available at /workspaces"
